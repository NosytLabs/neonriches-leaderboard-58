
import { useSoundHook } from './sounds/use-sound';

// Re-export the hook with the name useSound
export const useSound = useSoundHook;

// Default export for backward compatibility
export default useSoundHook;
